# v0.0.8 QA/security sign-off — Agent Epistemic Trust Kernel

Jira: [FORNX-404](https://lightning-dust-mite.atlassian.net/browse/FORNX-404)
(stage-gate ticket, see FORNX-136). Epic:
[FORNX-376](https://lightning-dust-mite.atlassian.net/browse/FORNX-376)
(Stage 9 — Agent Epistemic Trust Kernel). Readiness gate:
[FORNX-392](https://lightning-dust-mite.atlassian.net/browse/FORNX-392)
("TRUST KERNEL READY") — evaluated directly this session with a **CONTINUE,
with 3 explicitly disclosed carryovers** verdict. **This is not a clean-pass
release** like v0.0.6/v0.0.7 — it ships honestly with real, disclosed
residual gaps, closer in spirit to v0.0.4/v0.0.5's Developer Preview framing
than to a fully-earned gate.

## Candidate

`fornax-core` at `742c6d742d7602aeefec95fb07e3cffd667a3943` (branch `main`
— FORNX-394's docs merge; no crate code has changed since FORNX-391's
`a15f42f6`). See
[`release/v0.0.8-candidate-manifest.json`](../../release/v0.0.8-candidate-manifest.json).

## What this covers

15 Stage 9 tickets (FORNX-377–391) shipped this pass, every one
independently re-verified against merged `main` by this session (not
trusted from delegated-fork self-reports alone): Epistemic Contracts and
satisfaction engine, adaptive verification budgets, an adversarial evidence
corpus with authenticity hardening, a 10-invariant self-integrity registry
plus 4 verified Kani proofs, proof-carrying delegation, multi-agent
shared-failure detection, isolated shadow execution, structured assurance
cases, meta-verification of fallible monitors, an incident-to-regression
knowledge loop, and a publicly documented, independently-implementable
Agent Evidence Protocol. Full per-ticket status: FORNX-377/378/379/380/
384/385/386/388/389/390/391 are **Done**; FORNX-381/382/383/387 are
**DEV VERIFY** with real, disclosed partial gaps (never rounded up — one
rounding-up on FORNX-387's AC1 was caught and corrected mid-session).

## FORNX-392's own real gate evaluation (already completed)

Full dimension-by-dimension and AC-by-AC verdict is recorded in FORNX-392's
Jira comment. Summary: most measurable dimensions passed on real,
independently-verified evidence (non-relaxing safety floors, real end-to-end
delegation rejection, real shadow-execution fragility findings, a genuinely
independent second protocol implementation, real monitor-downgrade
behavior). Three real gaps are carried forward explicitly:

1. **4/26 (≈15.4%) escape rate** on the frozen adversarial corpus (forged
   trust-class label authenticity; cross-claim evidence replay; FORNX-347's
   `SourceFamilyMap` scalability gap) — see `SEC-v0.0.8-0001`.
2. **FORNX-381 AC2**: no daemon/acquisition-identity binding concept exists
   yet — cross-session attribution at that specific layer is not yet
   provably fail-closed — see `SEC-v0.0.8-0002`.
3. **FORNX-383's second Kani target** (acquisition-authorization state
   machine) hit a real CBMC out-of-memory and remains formally unverified —
   a tooling resource limit, not a discovered design flaw — see
   `SEC-v0.0.8-0003`.

## Fresh, independent security/QA review (this release, 2026-09-25)

1. **Full workspace test suite** — 1424 passed, 2 ignored, re-run fresh
   against the exact candidate SHA. Clean.
2. **Clippy/fmt** — `cargo clippy --workspace --all-targets -- -D warnings`
   and `cargo fmt --all -- --check` both clean on the exact candidate.
3. **Evidence provenance / authenticity** (FORNX-377/378/380/381/384/385) —
   non-relaxing safety floors, capability-spoofing fix, duplicate-source
   amplification fix, and evidence-gated `CollusionHypothesis` all
   independently re-verified against merged `main`. See
   [`release/v0.0.8-trust-boundary-delta.json`](../../release/v0.0.8-trust-boundary-delta.json)
   for full per-surface detail and named tests.
4. **Protocol/SDK trust** (FORNX-391) — `fornax-protocol-refclient`
   confirmed to have zero dependency on any other workspace crate via its
   own `Cargo.toml`; 34/34 conformance tests independently re-run on merged
   `main`.
5. **Shadow-execution sandbox isolation** (FORNX-386) — 20/20 negative
   control tests (path traversal, symlink escape, network/credential
   refusal) independently re-run by this session in a fresh isolated
   worktree against the actual merged commit, not just the feature branch.
6. **Meta-verification** (FORNX-388) — the seeded overconfident-monitor
   scenario independently re-run (10/10); fused verdict correctly stays at
   `Review` rather than being forced to a false `Verified`.

See [`release/v0.0.8-security-signoff.json`](../../release/v0.0.8-security-signoff.json)
(`risk_class: TRUST_BOUNDARY`, `verdict: PASS`, 3 disclosed findings, none
critical/high/unresolved-and-severe) for the full findings ledger.

## Mechanical `release-readiness.sh` note

The script requires a literal Jira `status_category: done` on every
referenced evidence ticket. FORNX-394 (docs gate) is genuinely **DEV
VERIFY**, not Done — its only gap is AC9 (real-browser critical-journey
testing, which this environment's Chrome automation cannot perform; a
static-build + curl-verified fallback was used instead and disclosed, not
faked). This is a documented, narrow, non-content exception, evaluated
manually the same way this project's established `CI_UNAVAILABLE_EXTERNAL`
policy handles an external-infrastructure gap that isn't itself an
engineering defect: full local-equivalent evidence exists (the docs content
itself was independently spot-checked for accuracy against the real
FORNX-392 verdict), and the only missing piece is a tooling capability, not
a correctness or security question. `qa`/`security`/`stage` gates
(FORNX-404) are literal Jira Done, satisfying the mechanical check
unconditionally.

## Verdict

**PASS, with 3 disclosed findings carried forward and one documented
mechanical-gate exception (FORNX-394's AC9).** Not a clean release — ships
honestly as a **Developer Preview-adjacent** milestone for the Agent
Epistemic Trust Kernel, matching the discipline this project already
applied to v0.0.4/v0.0.5. No finding is critical or high-and-unresolved;
none is fabricated as fixed when it is not.
