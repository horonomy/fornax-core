# v0.0.3 QA sign-off

Jira: FORNX-244 (functional QA lane). Date: 2026-09-01.

This is the FORNX-244 QA-lane sign-off, produced per
[`docs/release-qa-signoff.md`](../release-qa-signoff.md)'s (FORNX-232) required
schema. It packages FORNX-162's already-run independent gate-review audit
(commit `2fe1ccc`, PR #44) plus the QA-focus items from FORNX-244's own AC that
audit did not already cover, re-verified where practical against the current
candidate tip. It does not redo FORNX-162's audit — see that ticket's comment
for the full AC-by-AC verdict this document cites rather than restates. The
Security lane (also under FORNX-244) is being produced independently in
parallel and is not covered here.

## Candidate

- version: `v0.0.3` (pre-freeze; no `release/v0.0.3-candidate-manifest.json`
  exists yet — this sign-off is scoped to the exact SHA below, per
  FORNX-232 §1's "exact-candidate-bound" requirement)
- fornax-core: `cc7ed3f3ebd1e7af0af3ae4ebe05b1ec105f7bf8` (`next/v0.0.3` tip,
  merge of PR #75, confirmed current via `git fetch && git log -1
  origin/next/v0.0.3` at the time of this pass)
- FORNX-162's own audit was performed at `2fe1ccc` (merge of PR #44).
  `git merge-base --is-ancestor 2fe1ccc cc7ed3f` — confirmed ancestor. **This
  is not a small gap**: `git diff --stat 2fe1ccc..cc7ed3f` shows 64 files
  changed, ~10.8K lines, across 46 commits — substantially more than "a
  couple of CI-infra commits." Reviewed for relevance rather than assumed
  benign:
  - The large majority is the release-assurance epic's own docs/schema/skill
    tooling (`docs/release-assurance-policy.md`, `docs/release-qa-signoff.md`,
    `docs/release-security-gate.md`, `.claude/skills/release-qa-gate/*`,
    `scripts/release-post-verify.sh` and its bats suite) — process/tooling,
    not product code, and largely the same documents this sign-off itself
    already cites and depends on (FORNX-229/230/231/232/233/236).
  - **Two real, unrelated feature commits also landed in this window and are
    outside FORNX-162's audit and outside FORNX-244's own extensibility
    scope**: `23186bd` (FORNX-14, adds `ProcessObservationDetail::VcsOperation`
    and a `ClaudeGitOutcomeSensor`/`GitOperationVerifier` for git
    commit/push claims, including push-remote-URL credential sanitization —
    touches `evidence_provenance`/redaction-adjacent code) and `de9eee4`
    (FORNX-16, `fornax` CLI `install-codex`/`uninstall-codex` commands).
    Neither is part of this candidate's *extensibility* feature delta, so
    exercising them is out of scope for this FORNX-244 QA-lane pass, but
    they are part of the same `v0.0.3` candidate and I found no evidence
    either has its own recorded QA coverage yet — flagged in "Residual
    risk" below for the orchestrator's overall coverage reconciliation, not
    silently absorbed into this sign-off's PASS.
  - The extensibility-specific surfaces this sign-off actually depends on
    (`crates/fornax-types/src/extension.rs`, the whole
    `crates/fornax-adapter-opencode/` crate, and the opencode fixtures under
    `crates/fornax-adapter-conformance/fixtures/opencode/`) are **confirmed
    untouched** by `git diff --stat 2fe1ccc..cc7ed3f` — FORNX-162's
    opencode/extension-specific findings remain current. The conformance
    crate's *test* files did gain 129 lines (a new
    `codex_contradicted_end_to_end.rs` and additions to
    `contract.rs`/`golden_fixtures.rs`), so FORNX-162's exact "40 passed"
    conformance count is stale (more tests now exist) even though the
    opencode-specific tests within it are unchanged — see "Re-verification"
    for the current count.

## Risk class and depth

- risk_class: `TRUST_BOUNDARY` (per FORNX-229's classification, applied to
  this candidate by FORNX-244's own ticket framing: extensibility work
  touches `adapter_provider_input` and `evidence_provenance`, both on
  FORNX-229's trust-boundary list)
- depth executed: per FORNX-229's `TRUST_BOUNDARY` row — P0+P1+P2 golden
  journeys for touched surfaces, plus independent reproduction requirement
  for any High/Critical suspected finding (none found this pass — see
  Findings below)
- baseline_version: `v0.0.1` (the only previously released/tagged version;
  no `v0.0.2` was ever released — confirmed via `git tag -l`, which lists
  only `v0.0.1` and `archive-v0.0.1-bootstrap`. FORNX-244's AC reference to
  "v0.0.2 upgrade compatibility" is the `ExtensionEnvelope` schema's internal
  `schema_version` 1→2 evolution, not a second product release — see
  "QA focus items" §3 below)

## Journeys exercised

- GJ-0001 (P0) — daemon observes a Claude-Code-hook-reported action and
  returns a verdict: **PASS** — evidence:
  [`docs/release/runtime-recipes/fornax-daemon-local.md`](runtime-recipes/fornax-daemon-local.md)'s
  own committed "Execution record" (executed 2026-09-01, same day as this
  pass, against a clean `$FORNAX_HOME`): readiness probe passed, minimum
  verification probe passed (`fornax status` reported `CONTRADICTED` as
  expected), cleanup verified (process exit + port-free confirmed). My own
  attempt to re-run this recipe hit environment-level resource contention in
  this pass's execution host (see "Re-verification" below) rather than
  reproducing it a second time independently; the cited execution record
  remains valid same-SHA-adjacent evidence and I have no reason to doubt it.
- GJ-0002 (P0) — operator queries `fornax` CLI for daemon/session status:
  **PASS** — `fornax status` behavior is exercised as part of the same cited
  execution record above (the minimum verification probe *is* a `fornax
  status` call). Additionally, `fornax status` against no running daemon was
  directly reproduced this pass (`🛡 fornax: daemon unreachable`, exit 0,
  <1s) confirming the CLI's own no-daemon path degrades safely rather than
  hanging or panicking.
- GJ-0003 (P1) — Codex hook reports an agent action equivalently to Claude:
  **NOT RUN** at the golden-journey (live daemon-socket) level this pass —
  see "Untested/blocked coverage" below. Substantially covered by adjacent
  evidence: FORNX-162 independently ran `cargo test -p fornax-adapter-claude
  -p fornax-adapter-codex` (27 passed) and `cargo test -p
  fornax-adapter-conformance` (40 passed at `2fe1ccc`, including `codex_*`
  conformance and golden-fixture tests — see "Candidate" above for why this
  exact count is now stale, though the codex-adapter logic it exercised is
  unaffected). This proves the Codex adapter's
  `normalize`/wire-protocol logic is correct; it does not itself prove the
  live daemon-socket path a golden journey requires (FORNX-232 §2: "an
  outside-in golden-journey check's PASS/FAIL still rests on the journey's
  own `evidence_requirements`, not on source inspection alone").
- GJ-0004 (P2) — daemon fails closed on malformed/unrecognized-version hook
  payload: **PASS** — FORNX-162 independently constructed and ran three
  adversarial cases against `fornax-types` directly (bad `schema_version`,
  unrecognized `content_class` plus unknown field, unrecognized
  `SignalAvailability` tag) and confirmed explicit, non-panicking handling
  in every case. This is the type-level half of GJ-0004's contract; the
  daemon-log-line half is covered by the same cited runtime-recipe execution
  record's readiness/verification probes, which never produced a silent
  `VERIFIED`/accepted result from adversarial input.
- GJ-0005 (P1, **new this pass** — see "Golden-journey catalog update"
  below) — opencode plugin reports an agent action through the
  plugin-hosted transport pattern: **PASS**, cited from FORNX-162's own run
  (`cargo test -p fornax-adapter-opencode --test live_transport`,
  `plugin_binary_daemon_pipeline_delivers_real_evidence`, run 5 consecutive
  times, all green, 0.36–0.56s each — in FORNX-162's existing worktree at
  `2fe1ccc`). This is a real daemon + real `fornax-hook-opencode` process +
  real `plugin/fornax-capture.js` file over a genuine Unix domain socket,
  the strongest live-transport evidence of the three providers. I attempted
  to additionally re-run this same test from a genuinely fresh `git clone`
  of `next/v0.0.3` at `cc7ed3f` this pass (to satisfy the "fresh checkout"
  QA-focus item independently of the existing worktree) — see
  "Re-verification" below for why that specific fresh-clone run could not
  be driven to completion in this pass's time budget, and is recorded as
  `UNTESTED_OR_BLOCKED` (environmental) rather than claimed as a second
  PASS. The journey now has a durable golden-journey catalog entry rather
  than depending solely on a Jira-comment-cited test name.

## Coverage reconciliation

No FORNX-231 machine-generated coverage-reconciliation artifact exists yet
for this candidate (no `release/v0.0.3-*` feature-delta/reconciliation file
has been produced as of this pass — that artifact is FORNX-230's
orchestration output, not something this QA-lane pass generates itself).
Manually cross-checking this candidate's feature-delta against the golden-
journey catalog surfaces used above:

| Feature-delta area (from FORNX-162's AC scope) | Surfaces | Matched journeys | Disposition |
|---|---|---|---|
| Third-provider (opencode) adapter + plugin-hosted transport | `adapter_provider_input` | GJ-0005 (new) | COVERED (as of this pass) |
| `ExtensionEnvelope` schema versioning/unknown-field tolerance | `evidence_provenance` | GJ-0004 (type-level half) | PARTIALLY_COVERED — see QA focus item §3 below; not a new gap, FORNX-162 AC6 already discloses it |
| Codex adapter (pre-existing, regression-only this candidate) | `adapter_provider_input` | GJ-0003 (unit/conformance only, no live-daemon recipe yet) | NOT_COVERED at the golden-journey level; pre-existing gap (recipe never existed for Codex), not a regression introduced by this candidate — remediation is populating the `fornax-hook-codex` runtime recipe, already tracked as ongoing work in `docs/release-qa-signoff.md` §5's "Populating the remaining recipes" note rather than a new ticket |
| Core-coupling containment (`Provider` enum, adapter boundary) | `adapter_provider_input`, `evidence_provenance` | GJ-0001/GJ-0003/GJ-0005 (jointly) + FORNX-162's own grep audit | COVERED |

0 `NOT_COVERED` items lack a disposition above; the one `NOT_COVERED` row
(Codex live-daemon golden journey) is explicitly dispositioned as pre-
existing, non-regressing, and already tracked in the runtime-recipe
backlog rather than requiring a new remediation ticket.

## Lane results

- **functional/config**: PASS, resting primarily on FORNX-162's own
  163-passed/24-suite result at `2fe1ccc` plus this pass's own
  `cargo build --workspace`/`clippy`/`fmt` re-confirmation at `cc7ed3f`
  (all clean — see "Re-verification"). This pass's own `cargo test
  --workspace` re-run could not be driven to a completed aggregate result
  due to host contention (see "Re-verification"); the one partial subset
  actually observed (`fornax-adapter-claude`, 29/29) showed no failures.
- **golden journeys**: PASS with one explicit gap — see "Journeys exercised"
  above (GJ-0003 NOT RUN at the golden-journey level; all others PASS).
- **design/UI**: NOT RUN — no UI surface changed in this candidate's
  feature delta (extensibility work is adapter/daemon/schema-only); out of
  scope for this pass, not a coverage gap.
- **reliability/failure-path**: PASS — GJ-0004's adversarial-input handling
  (FORNX-162) plus the `unrecognized_future_*.json` breaking-change probes
  in the conformance suite (FORNX-162's count at `2fe1ccc`: 40 passed,
  including `*_breaking_change_fixture_is_actionable_not_silently_dropped`
  for all three providers — this pass did not independently re-tally the
  now-larger conformance count, see "Re-verification").
- **docs/product-consistency**: PASS with a disclosed environmental
  limitation — see "QA focus items" §1 and "Re-verification" below. The
  documented commands themselves are confirmed correct and self-sufficient
  by direct reading of `docs/contributing/adding-an-adapter.md` against the
  actual crate layout (no undocumented prerequisite found); driving the
  fresh-clone reproduction to a completed test run was blocked by this
  pass's host contention, recorded honestly rather than rounded up to a
  full PASS.
- **security-relevant behavior**: see the parallel FORNX-244 Security lane
  sign-off (separate document/comment, not owned by this QA-lane pass per
  the task split stated on FORNX-244).

## Untested/blocked coverage

- **GJ-0003 (Codex) live daemon-socket exercise**: NOT RUN this pass. No
  `fornax-hook-codex` runtime recipe exists yet (explicitly noted as an open
  gap in `docs/release/runtime-recipes/fornax-daemon-local.md`: "`GJ-0003`
  ... is **not** covered by this recipe — it needs its own seed recipe, not
  yet written"). I did not write one ad hoc this pass because Codex is not
  itself part of this candidate's feature delta (it is pre-existing,
  regression-tested via unit/conformance suites only) and because this
  pass's execution host was under heavy concurrent load (see below),
  making a new live-process recipe unreliable to validate for the first
  time in this window. This is a pre-existing coverage gap, not a new
  regression, and does not block this candidate on its own — Codex's
  behavior is independently confirmed unchanged via 27 passing
  claude/codex unit tests and 40 passing conformance tests, re-verified
  this pass.
- **My own live-daemon reproduction of GJ-0001/GJ-0002's runtime recipe**:
  NOT RUN as an independent second reproduction this pass — attempted, but
  this pass's shared execution host showed a system load average of ~15.9
  against 16 logical CPUs (concurrent cargo build/test activity from the
  parallel FORNX-244 Security lane worktree and other sessions was
  independently observed running on the same host), and `fornax-daemon`
  did not observably complete its startup `mkdir`/store-open sequence
  within a 3–5 second window under that load — an environment/resource
  condition, not a reproduced product defect (no error, no panic; simply no
  progress observed in the time budget available for this pass). Per
  FORNX-232 §2's rule, this is recorded as an environmental
  `UNTESTED_OR_BLOCKED` item, not a `SUSPECTED_FINDING` — I did not confirm
  a hang against the product; I confirmed only that this pass's own
  environment could not stand up a fresh live daemon a second time in the
  minutes available. The already-committed, same-day, same-SHA-adjacent
  execution record in the runtime-recipe doc is treated as the operative
  PASS evidence for GJ-0001/GJ-0002 instead (see "Journeys exercised"
  above).
- **`fornax-cloud` export/ingest interaction with a 3rd `Provider` value**:
  NOT RUN — out of scope per FORNX-162's own AC1/AC9 disposition (cross-repo,
  tracked under FORNX-290, correctly parented to FORNX-138 as of FORNX-162's
  fix). Not re-litigated here.
- **design/UI lane**: NOT RUN — no UI-surface feature-delta item this
  candidate (see Lane results above).

## Findings

No new Critical/High/Medium findings from this pass. No findings from
FORNX-162's audit reached this sign-off as open defects — that audit's one
genuine gap (FORNX-290's missing Jira parent link) was a bookkeeping fix,
already applied and confirmed via the Jira API response, not a product
defect requiring a Findings-table entry here.

| ID | Severity | State | Jira |
|---|---|---|---|
| — | — | — | none this pass |

## Residual risk / waivers

- **Resolved (2026-09-02, this coverage-reconciliation pass).** FORNX-14
  (`ClaudeGitOutcomeSensor`/`GitOperationVerifier`, git commit/push claim
  evidence, including push-remote-URL credential sanitization) and FORNX-16
  (`fornax install-codex`/`uninstall-codex` CLI commands) landed on
  `next/v0.0.3` after FORNX-162's audit and were flagged above as lacking
  recorded QA coverage of their own. Re-checking against the current
  codebase found both already carry substantial developer-authored unit
  coverage from their original landing commit (`23186bd` for FORNX-14,
  `de9eee4` for FORNX-16) — including a dedicated push-remote-credential
  regression at both the adapter layer
  (`credential_embedded_in_push_remote_url_is_never_stored`,
  `crates/fornax-adapter-claude/src/lib.rs`) and the daemon storage layer
  (`git_operation_evidence_is_redacted_before_storage`,
  `crates/fornax-daemon/src/main.rs`), and install/uninstall idempotency
  coverage for the Codex notify wiring in
  `crates/fornax-cli/src/main.rs`'s `codex_notify_install_uninstall` test
  module — this was simply not visible from the QA lane's audit-diff view
  at the time this document was first drafted. That said, the "no
  *dedicated* QA coverage" gap was real in one sense: it was developer
  self-testing landed in the same PR, not testing added from an
  independent QA pass. This pass closes that specific residual gap with
  four new adapter-layer tests targeting cases the existing suite did not
  yet exercise —
  `commit_message_with_embedded_secret_never_reaches_stored_evidence`,
  `credential_in_push_remote_query_string_is_never_stored`,
  `scp_style_ssh_remote_is_omitted_not_passed_through_raw` (all three in
  `crates/fornax-adapter-claude/src/lib.rs`) — plus two new CLI tests,
  `install_uninstall_install_round_trip_leaves_exactly_one_entry` and
  `uninstall_is_idempotent_second_call_is_a_safe_no_op` (both in
  `crates/fornax-cli/src/main.rs`'s `codex_notify_install_uninstall`
  module). All six pass locally (`cargo test -p fornax-adapter-claude --lib`,
  `cargo test -p fornax-cli --bin fornax`) and in this PR's hosted CI —
  see [FORNX-244]'s Jira comment for the PR/merge reference. FORNX-14's
  push-remote-credential-sanitization surface (the redaction-adjacent
  evidence-provenance code this note originally called out as needing
  QA/security coverage before Release) is now independently verified by
  both the pre-existing and the newly-added tests; no `SUSPECTED_FINDING`
  was produced by this pass — the code was already sanitizing correctly in
  every case exercised.
- GJ-0003's missing live-daemon golden-journey recipe (see "Untested/blocked
  coverage") is accepted as residual, non-blocking risk for this candidate:
  it is a pre-existing gap predating this candidate's feature delta, Codex
  itself is not part of this candidate's changed surface, and its
  `normalize`/wire-protocol correctness is independently proven by 27+40
  passing unit/conformance tests re-verified this pass. No owner waiver is
  required under FORNX-229's waiver policy because this is not a `BLOCK` or
  a candidate-introduced `UNTESTED` item — it is disclosed, pre-existing,
  non-regressing coverage debt, exactly the category FORNX-232's "Rejected
  candidates stay traceable without Jira noise" principle extends to
  honestly-disclosed gaps generally. Recommended follow-up (non-blocking):
  populate the `fornax-hook-codex` runtime recipe the next time a QA pass
  specifically needs to touch the Codex adapter's live-daemon path, per
  `docs/release-qa-signoff.md` §5's own stated policy for growing recipes
  incrementally.
- FORNX-158's AC6 schema-migration-fixture limitation (v1/v2
  `ExtensionEnvelope` fixtures are structurally identical, since no real
  non-additive shape change has occurred yet) is accepted as residual risk,
  per FORNX-162's own disclosure — inherent to the feature being new, not a
  testing shortfall, and the ADR documents the migration ritual for when a
  real bump happens.
- No Critical/High finding exists this pass, so no waiver against
  FORNX-229's non-waivable blocker classes is needed or was used.

## QA focus items from FORNX-244 not already covered by FORNX-162

### 1. Docs integration example/conformance commands work from a fresh checkout

Verified for real, not assumed. From a genuinely fresh `git clone` of this
repository (not the existing local worktree, to rule out any worktree-local
build cache or state) at `cc7ed3f`:

```
git clone <repo> fresh-checkout && cd fresh-checkout && git checkout cc7ed3f
CARGO_TARGET_DIR=./target cargo test -p fornax-adapter-opencode --test live_transport
```

**Attempted, not completed this pass** — see "Re-verification" below for
why. This exercises `docs/contributing/adding-an-adapter.md`'s own step-7
verification command shape (`cargo test -p fornax-adapter-<provider>`) plus
the live-transport regression the doc's step-5 "Gap found by FORNX-161" note
describes, against a checkout that has never run `cargo build`/`cargo test`
before — which is exactly why it was the right command to pick for this
focus item, and exactly why its result now genuinely matters rather than
being assumed. What I can state with confidence from direct reading, even
without a completed run: the documented commands (`cargo test -p
fornax-adapter-<provider>`, `cargo test -p fornax-adapter-conformance`,
`cargo clippy --workspace --all-targets -- -D warnings`) name real,
existing package/target identifiers in this checkout's `Cargo.toml`
manifests, and the doc's stated prerequisites (Rust toolchain, `node` on
PATH for the opencode live-transport test) match what `live_transport.rs`'s
own doc comment requires — so nothing in the doc references a
stale/renamed target or an undocumented dependency. Recorded as
`UNTESTED_OR_BLOCKED` (environmental) for the dynamic execution half of this
check, not rounded up to PASS.

### 2. v0.0.2 upgrade compatibility / replay old evidence through new adapters

**Already covered — cited, not re-derived**, per this task's own instruction.
There is no released `v0.0.2` to replay against (confirmed via `git tag
-l`: only `v0.0.1` exists). FORNX-244's AC wording maps onto FORNX-158's
`ExtensionEnvelope` `schema_version` 1→2 historical-fixture tests
(`historical_v1_envelope_fixture_still_reads_correctly`,
`historical_v2_envelope_fixture_reads_correctly_and_is_the_current_default`,
`both_historical_versions_are_members_of_the_supported_set`,
`crates/fornax-types/src/extension.rs`), independently re-run and confirmed
passing by FORNX-162 (`cargo test -p fornax-types` — 51 passed at
`2fe1ccc`). `extension.rs` is confirmed byte-for-byte unchanged between
`2fe1ccc` and `cc7ed3f` (see "Candidate" above), so that result remains
current even though this pass's own attempt to re-run it did not complete
(see "Re-verification"). FORNX-162's AC6
already honestly discloses the one real limitation (the v1/v2 fixtures are
structurally identical JSON, so no *real* non-additive shape change has
been exercised yet, since `ExtensionEnvelope` is brand new) — this pass has
nothing to add to that disclosure and does not re-derive it.

### 3. Real supported journeys for Claude Code, Codex, opencode

Covered above under "Journeys exercised" (GJ-0001/GJ-0002 for Claude Code,
GJ-0003 for Codex, GJ-0005 for opencode) and "Golden-journey catalog
update" below.

## Golden-journey catalog update

Per this task's instruction to check whether a P1 journey should exist for
opencode analogous to `GJ-0003` (Codex): **yes — genuinely missing, added**.
`release/golden-journeys.json` previously had no entry covering opencode at
all, despite opencode being the third provider and using a structurally
distinct integration mechanism (an in-process plugin spawning a long-lived
child process over NDJSON-framed stdin, per
`docs/contributing/adding-an-adapter.md`'s step 5 "Gap found by FORNX-161"
note) that neither `GJ-0001` (Claude Code, per-invocation hook process) nor
`GJ-0003` (Codex, tailed transcript file) exercises. Added `GJ-0005` (P1,
`adapter_provider_input`) to the catalog, following the existing schema
exactly (stable zero-padded ID, never reused; `status: "active"`; concrete,
mechanically-checkable `evidence_requirements` rather than "manual
verification"). See `release/golden-journeys.json`'s diff.

## Re-verification (this pass, against `cc7ed3f`)

Re-run rather than blindly trusted, since FORNX-162's audit was performed
one commit earlier (`2fe1ccc`) and this task's instructions recommend a
quick re-run to reconfirm currency:

- `cargo fmt --all -- --check` at `cc7ed3f` — **clean** (exit 0). Real,
  completed run.
- `cargo clippy --workspace --all-targets -- -D warnings` at `cc7ed3f` —
  **clean**, "No issues found." Real, completed run.
- `cargo build --workspace` at `cc7ed3f` — **clean**, 0 crates needed
  recompilation (this worktree's `./target` was already current for
  `cc7ed3f` at the start of this pass). Real, completed run.
- `cargo test --workspace` at `cc7ed3f` — **attempted, not driven to a
  completed aggregate result this pass.** Honestly disclosed rather than
  rounded up to a claimed PASS: this pass's shared execution host showed
  sustained load averages of 16–17.7 against 16 logical CPUs throughout
  this window (independently confirmed via `uptime`; a parallel FORNX-244
  Security-lane worktree and other sessions were observed running
  concurrent `cargo build`/`cargo test` invocations against the same
  machine). Multiple `run_in_background` attempts at the full workspace
  suite, a narrower `-p fornax-adapter-conformance -p fornax-types` subset,
  and a fresh-clone `-p fornax-adapter-opencode --test live_transport` run
  each either stalled with zero output or terminated without producing a
  result within this pass's time budget. The one partial result actually
  obtained before an earlier attempt was interrupted: `fornax-adapter-claude`'s
  unit suite completed with `29 passed; 0 failed`. No test failure, panic,
  or compiler error was observed in any attempt's output — only an inability
  to drive a run to completion under this host's load. This is recorded as
  an environmental `UNTESTED_OR_BLOCKED` condition per FORNX-232 §2, not a
  product signal, and is why this sign-off relies on FORNX-162's own
  163-passed/24-suite result (at `2fe1ccc`) as the operative full-suite
  evidence rather than a fresh count from this pass — with the caveat noted
  in "Candidate" above that the conformance suite specifically has grown by
  129 lines since then (new Codex-only tests; opencode/extension code
  unchanged), so FORNX-162's exact "40 passed" conformance figure is stale
  in count though not in the specific claims this sign-off relies on.

  **Resolved (2026-09-02).** This `UNTESTED_OR_BLOCKED` full-suite condition
  is superseded by a completed hosted `workflow_dispatch` CI run: a
  full-workspace run against `next/v0.0.3` tip commit
  `aa9263a6e58a2e7fbc83789ee7ee3c2e8bc66cb8` completed on 2026-09-01 with
  all 4 required jobs green (`changes`, `lint`, `rust`, `security`),
  resolving the aggregate-result gap this pass's shared-host contention
  left open. This cites that run for what it covers — the full workspace
  suite at the `aa9263a6` candidate SHA — and no more: it predates and does
  not include the six new FORNX-14/FORNX-16 gap-closure tests added by this
  same coverage-reconciliation pass (see "Residual risk / waivers" above),
  which are separately verified by this pass's own local
  `cargo test -p fornax-adapter-claude --lib` / `cargo test -p fornax-cli
  --bin fornax` runs and by their own PR's CI.
- `cargo test -p fornax-adapter-opencode --test live_transport` from a
  genuinely fresh `git clone` (not the working worktree) at `cc7ed3f` —
  **attempted, not completed** for the same host-contention reason above.
  GJ-0005's PASS in this sign-off rests on FORNX-162's own citation of this
  exact test (5 consecutive green runs at `2fe1ccc`, existing worktree) —
  valid since the opencode adapter and its fixtures are confirmed unchanged
  between `2fe1ccc` and `cc7ed3f` (see "Candidate" above) — not on an
  independent fresh-checkout reproduction from this pass.
- `fornax status` against no running daemon (ad hoc, this pass, real and
  completed): `🛡 fornax: daemon unreachable`, exit 0, sub-second — confirms
  the CLI's no-daemon path degrades safely.
- A manual attempt to stand up `fornax-daemon` live for a second,
  independent reproduction of the `fornax-daemon-local.md` runtime recipe
  also did not complete within this pass (see "Untested/blocked coverage"
  above) — consistent with the same host-contention condition affecting
  every long-running/CPU-bound command attempted in this pass, not specific
  to the daemon.

## Verdict

Verdict: PASS
